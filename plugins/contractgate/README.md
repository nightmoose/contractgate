# ContractGate plugin (Claude Code / Cursor / Codex)

Install the marketplace, then the plugin:

```
/plugin marketplace add nightmoose/contractgate
/plugin install contractgate@contractgate
```

Requires `CONTRACTGATE_API_KEY` in the environment. Playbook: https://app.datacontractgate.com/llm-integration.md
