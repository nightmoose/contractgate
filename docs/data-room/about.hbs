# Third-party Rust dependency licenses

Generated by `cargo-about` for diligence. **Do not hand-edit** the inventory table;
regenerate with:

```bash
cargo about generate docs/data-room/about.hbs -o docs/data-room/dependency-licenses.md
```

Config: `about.toml` at repo root.

---

## Overview

{{#each overview}}
- **{{id}}**: {{count}}
{{/each}}

## Accepted licenses (policy)

See `about.toml` `accepted` list (MIT, Apache-2.0, BSD variants, ISC, MPL-2.0, etc.).

## Crates by license

{{#each licenses}}
### {{name}}

{{#each used_by}}
- `{{crate.name}}` {{crate.version}}
{{/each}}

{{/each}}

## Notes

- This inventory covers the **Rust** workspace used to build `contractgate-server`.
- Dashboard (`dashboard/package.json`) is a separate npm tree; scan with
  `license-checker` or similar if a counterparty requires it.
- Production JWT path: see `deny.toml` advisory ignore for RUSTSEC-2023-0071
  (jsonwebtoken → rsa, verify-only).
