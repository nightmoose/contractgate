# ContractGate Audit Punchlist — 2026-05-02

Full sweep of repo, CI, app, and marketing site. Items ordered by severity.

---

## 🔴 Critical (blocking correctness or CI green)

### ✅ 1. Compose smoke CI — `GET /contracts` 500 — RESOLVED (2026-05-02)
**Root cause:** `ops/postgres/initdb-wrapper.sh` was committed as `100644` (not executable).
Docker's `/docker-entrypoint-initdb.d/` only runs `.sh` files with the executable bit set —
so migrations never applied, `contracts` table didn't exist, every DB query returned
`ERROR: relation "contracts" does not exist` → 500.  
**Fix:** `c29391d` changed the git mode to `100755`. Confirmed: `git ls-files -s` shows
`100755`. Tests re-enabled in `4555dba`. No further action needed — CI will verify on
next push to main.

---

### 2. PyPI deployment — almost certainly broken
**Where:** `.github/workflows/release.yml` → `publish-pypi` job  
**What:** This job runs on every tag push AND every `workflow_dispatch`. It always tries to
publish to TestPyPI (requires `TEST_PYPI_API_TOKEN` secret). The SDK is still marked
`0.1.0 (unreleased)` in `sdks/python/CHANGELOG.md` — it has never actually shipped.
PyPI proper uses OIDC Trusted Publisher which requires one-time GitHub→PyPI setup that
is not confirmed done. This is almost certainly the "old error" you saw.  
**Next steps:**
1. Confirm `TEST_PYPI_API_TOKEN` secret is set in the repo.
2. Confirm the PyPI Trusted Publisher is registered (`repo=nightmoose/contractgate`,
   `workflow=release.yml`, `environment=pypi`).
3. Update `sdks/python/CHANGELOG.md` — remove `(unreleased)` when ready to ship.
4. Run a `workflow_dispatch` with a test tag to smoke-test the full pipeline before a real
   release.

---

## 🟠 High (user-facing bugs or broken workflows)

### 3. YAML editor — dark text on dark background (known since 2026-04-20)
**Where:** `dashboard/app/contracts/_tabs/yaml.tsx` line ~144  
**What:** Browser test on 2026-04-20 confirmed YAML textarea content is invisible in the
contract edit modal. Root cause: when the version is not a draft, the textarea uses
`text-slate-400` on `bg-[#0a0d12]/70` — low contrast. During initial load (before
`yamlDraft` is set), the `isDraft` branch may also render with wrong contrast.  
**Fix:** Verify the `isDraft` / read-only path renders `text-slate-300` or similar; add a
CSS contrast check. Also confirm `value={yamlDraft}` is never `undefined` (would render
as empty).

---

### 4. Domain fragmentation — four different hostnames in production files
**Where:** across `marketing/index.html`, `README.md`, `dashboard/app/**`, `sdks/python/`,
`SECURITY.md`, `confluent-connector/README.md`  
**What:** The project uses four different domains inconsistently:

| Domain | Files |
|---|---|
| `app.contractgate.io` | `marketing/index.html`, `dashboard/app/pricing/page.tsx` |
| `contractgate.dev` | `sdks/python/README.md`, `SECURITY.md` |
| `datacontractgate.com` | `README.md`, `dashboard/app/auth/signup/page.tsx`, confluent README |
| `app.datacontractgate.com` | `README.md` (live demo link) |

**Fix:** Decide the canonical domain, do a grep-and-replace pass across all non-`.next/`
files. Likely `datacontractgate.com` is the live one (browser test was run against it).

---

## 🟡 Medium (polish, debt, stale content)

### 5. Marketing site — dead Privacy and Terms links
**Where:** `marketing/index.html` lines 867–868  
**What:** Both footer links are `href="#"`. No Privacy Policy or Terms of Service pages
exist anywhere in the repo.  
**Fix:** Either write stub pages and link them, or route to a legal subdomain. Needed before
any public launch.

### 6. README — stale pip install comment
**Where:** `README.md` line ~38  
**What:** `## pip install contractgate  # coming to PyPI this week` — written April 26, still
not shipped. Also: `(Upload these to a /screenshots/ folder in the repo)` — screenshots
folder exists and is populated; this note should be removed.

### 7. `sdks/python/dist/` checked into git
**Where:** `sdks/python/dist/contractgate-0.1.0-py3-none-any.whl` and `.tar.gz`  
**What:** Built artifacts in version control. The release workflow builds them fresh anyway.  
**Fix:** Add `sdks/python/dist/` to `.gitignore`, delete from repo.

### 8. `sdks/python/pytest-cache-files-i5hfi5x_/` checked into git
**Where:** `sdks/python/pytest-cache-files-i5hfi5x_/`  
**What:** Stray pytest cache directory committed to the repo.  
**Fix:** Delete + add `pytest-cache-files-*/` to `.gitignore` (or ensure `.pytest_cache/`
covers it).

### 9. Security advisories accumulating in audit.toml
**Where:** `.cargo/audit.toml`  
**What:** 8 RUSTSEC advisories are suppressed, including four from 2026
(`RUSTSEC-2026-0097` rand unsound, `RUSTSEC-2026-0098/0099/0104` rustls-webpki).
Each suppression has no expiry comment or ticket.  
**Fix:** Add a comment to each ignore entry with the date suppressed and a link to the
upstream issue. Periodically prune — especially rand (0.8.x) when 0.9 stabilizes.

### 10. Dockerfile installs `libssl-dev` unnecessarily
**Where:** `Dockerfile` line 15  
**What:** The codebase uses `runtime-tokio-rustls` (pure Rust TLS) in sqlx and `rustls-tls`
in reqwest. `libssl-dev` is a build-time OpenSSL header — not needed at all.  
**Fix:** Remove `libssl-dev` from the builder stage. The runtime stage already has `libssl3`
for any dynamic linking, and rustls doesn't need it.

---

## 🔵 Low / Housekeeping

### 11. TypeScript SDK (RFC-018) not yet scaffolded
**Where:** `docs/rfcs/018-typescript-sdk.md` exists, `sdks/` only has `python/`  
**What:** RFC is written, implementation not started.  
**Note:** Track when Python SDK ships — TS SDK is logically next.

### 12. No `.sqlx/` query cache in repo
**Where:** `.sqlx/` directory is empty  
**What:** CI `rust-check` runs with `SQLX_OFFLINE=true`. Since all `query!` macros were
converted to runtime queries (MAINTENANCE_LOG), this doesn't break anything today. But if
a `query!` macro is ever re-introduced, CI will silently fail at compile time with a confusing
error.  
**Fix:** Either commit a note in `.sqlx/README.md` explaining this is intentional, or run
`cargo sqlx prepare` against a dev DB and commit the generated metadata.

### 13. `Grok_Suggestions.txt` in repo root
**Where:** `Grok_Suggestions.txt`  
**What:** Appears to be AI-generated suggestions kept as a scratch file. Doesn't belong in
the repo root.  
**Fix:** Move to `docs/` or delete.

---

## Summary counts

| Severity | Count |
|---|---|
| 🔴 Critical | 2 |
| 🟠 High | 2 |
| 🟡 Medium | 6 |
| 🔵 Low | 3 |
| **Total** | **13** |
